# PartyKit host for the live reactions widget.
# After deploying the server (`npx partykit deploy`), set this to the deployed host, e.g.:
# VITE_PARTYKIT_HOST=bhavishya-reactions.<your-partykit-username>.partykit.dev
VITE_PARTYKIT_HOST=
