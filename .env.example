# Cloudflare Worker host for the live reactions widget.
# After deploying the worker (`npx wrangler deploy -c worker/wrangler.jsonc`), set this to the deployed host:
# VITE_REACTIONS_HOST=bhavishya-reactions.<your-subdomain>.workers.dev
VITE_REACTIONS_HOST=
